{#
  Страница «Темы карточек». Рендерится scripts/build_theme_preview.py в
  themes/preview.html; правьте шаблон, не результат.

  Экранирование включено, поэтому srcdoc карточек и CSS в раскрывающихся блоках
  подставляются как есть — Jinja сама превратит кавычки и скобки в сущности.

  А вот стиль и скрипт самой страницы идут через |safe: это наши же файлы, и внутри
  <style>/<script> сущности не раскрываются. Без |safe кавычки в них становятся
  &#39; и &#34;, отчего скрипт перестаёт разбираться, а селекторы вида
  [data-theme="dark"] и шрифтовые стеки — совпадать.

  Документ полный, с head и meta charset, а не фрагмент: файл открывают прямо из
  репозитория, в том числе через локальный сервер IDE. Тот отдаёт его без charset
  в заголовке, и Firefox без meta показывает кириллицу кракозябрами.
#}
<!doctype html>
<!-- Страница собрана scripts/build_theme_preview.py; правьте CSS в
     src/anki_deck_gen/notetypes/assets/css и пересоберите её. -->
<html lang="ru">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>Темы карточек anki-deck-gen</title>
<style>{{ page_css | safe }}</style>
</head>
<body>
<main>
<header>
  <div>
    <h1>Темы карточек anki-deck-gen</h1>
    <p class="lead">Одна и та же запись во всех оформлениях, которые предлагает бот.
    Переключатель показывает ночной режим Anki.</p>
  </div>
  <button class="switch" id="night" type="button" aria-pressed="false">
    <span class="knob" aria-hidden="true"></span>Ночной режим</button>
</header>
<div class="board"><div class="grid">
  <div class="corner"></div>
  {%- for column in columns %}
  <div class="colhead">
    <div class="colname">{{ column.name }}{% if column.chip %}<span class="chip">{{ column.chip }}</span>{% endif %}</div>
    <div class="colnote">{{ column.note }}</div>
  </div>
  {%- endfor %}
  {%- for row in rows %}
  <div class="rowhead">
    <span class="rowname">{{ row.name }}</span>
    <span class="rownote">{{ row.note }}</span>
  </div>
  {%- for cell in row.cells %}
  <iframe class="cardframe" title="{{ cell.title }}" srcdoc="{{ cell.document }}"></iframe>
  {%- endfor %}
  {%- endfor %}
</div></div>
<section class="notes">
  <div>
    <h2>Общее у тем</h2>
    <ul>
      <li>Меняется только CSS типа записи. Поля, шаблоны и model_id прежние, поэтому
      Anki обновит оформление и в уже импортированных колодах.</li>
      <li>Шрифты только системные: веб-шрифт пришлось бы класть файлом в каждую колоду.</li>
      <li>Кнопка озвучки — контурный круг в цвете темы вместо стокового залитого.</li>
      <li>Ночной режим по классу card nightMode, который ставят Anki, AnkiDroid и
      AnkiMobile.</li>
      <li>Подсветка набранного ответа мягче стоковой: вместо чистых зелёного и красного.</li>
    </ul>
  </div>
  <div>
    <h2>Чем отличаются</h2>
    <ul>
      <li><strong>Карточка.</strong> Узнаваемый вид карточки: светлая панель с тенью.
      Фон вокруг рисуется тенью, поэтому работает и в старых версиях Anki.</li>
      <li><strong>Учебник.</strong> Антиква и двойная линейка, как в печатном пособии.
      На Android засечки заменит Noto Serif.</li>
    </ul>
  </div>
</section>
<section class="notes" style="grid-template-columns: 1fr">
  <div>
    <h2>CSS</h2>
    {%- for column in columns if not column.chip %}
    <details><summary>CSS темы «{{ column.name }}»</summary><pre><code>{{ column.css }}</code></pre></details>
    {%- endfor %}
  </div>
</section>
</main>
<script>{{ page_js | safe }}</script>
</body>
</html>
