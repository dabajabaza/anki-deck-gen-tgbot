# Словарь

Слова, которыми говорят код, логи и ответы бота. Термины Anki — из его русской
локализации (`anki-core-i18n/core/ru/*.ftl`): **Запись**, **Тип записи**, **Карточка**,
**Колода**, **Метка**. Не «заметка», не «тег», не «пресет».

## Люди

- **Админ** — человек из `ADMIN_IDS`. Допущен без строки в базе. Единственный, кто
  раздаёт доступ: выпускает Инвайты и допускает по id.
- **Гость** — допущен по Инвайту или командой Админа. Делает колоды на равных с
  Админом; приглашать не может.
- **Посторонний** — все остальные. Получает молчание: любой ответ подтвердил бы,
  что бот существует.
- **Инвайт** — одноразовая ссылка `t.me/<bot>?start=<код>`, живёт 48 часов.
  Погашенная — мёртвая.

## Таблица

- **Таблица** — то, из чего делается колода. Приходит из **Источника**: файл
  `.xlsx`/`.csv`, ссылка на Google Таблицу или вставленный текст.
- **Лист** — вкладка Таблицы. У csv и текста Лист один и безымянный.
- **Заголовок** — первая строка Листа: колонки `Q` и `A` (или `Вопрос` и `Ответ`)
  обязательны; `Deck`/`Колода` и `Tags`/`Метки` — по желанию; прочие колонки
  сохраняются по имени для кастомных Типов записи. У вставленного текста заголовка
  нет: строка = запись, «вопрос / ответ» (слэш с пробелами или табуляция).
- **Проблемная строка** — строка, из которой нельзя сделать Запись: пустой вопрос,
  пустой ответ, нет разделителя в тексте. Решается одним из трёх способов:
  исправить в диалоге, пропустить, отменить всё.
- **Дубль** — вопрос, встретившийся в Таблице дважды. Не проблема, а предупреждение:
  Anki сам пометит его при импорте.

## Колода

- **Тип записи** — тип записи Anki, взятый как есть: `Простая`,
  `Простая (с обратной карточкой)`, `Простая (с вводом ответа)`, плюс собранный
  из них `Простая (с вводом ответа в обе стороны)`. Кастомный тип,
  добавленный в код (вьетнамский с раскраской тонов), — тоже Тип записи и виден
  в боте наравне со стоковыми.
- **Языки** — язык вопроса и язык ответа; нужны озвучке. **Озвучка** — какая сторона
  озвучена: нет, вопрос, ответ, обе.
- **Оформление** — как выглядят Карточки: тема «Карточка» (светлая панель с тенью на
  сером фоне) или «Учебник» (антиква на бумажном фоне). Только CSS Типа записи.
- **Настройки колоды** — Тип записи + Языки + Озвучка + Оформление. Всё, что бот
  спрашивает перед сборкой. Последние Настройки человека запоминаются — кнопка
  «Как в прошлый раз».
- **Запись** (note) — строка Таблицы, ставшая единицей Anki. **Карточка** (card) — то,
  что Anki показывает; «с обратной карточкой» даёт две Карточки на Запись.
- **Колода / Подколода** — иерархия Anki через `::`. Лист становится Подколодой,
  если Листов больше одного; колонка `Deck` — тоже Подколода; вместе — `Лист::Deck`.
- **Метка** — tag Anki, из колонки `Tags`/`Метки` через запятую.

## Работа

- **Задание** — одна Таблица с Настройками от одного человека. Занимает одно место
  в очереди, живёт до Вердикта.
- **Вердикт** — чем кончилось Задание: файл с колодой и сводкой или объяснение, почему
  не вышло. Статус-сообщение всегда доходит до Вердикта; молчание — не исход.
