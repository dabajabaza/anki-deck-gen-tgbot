"""Единые часы приложения: одна функция «сейчас» на весь пакет.

Лежит в корне пакета, а не в подпакете: время читают и db/models.py (default
колонок), и services/, и bot/pending.py. Модуль не импортирует ничего своего и
поэтому лежит ниже всех слоёв.
"""

import time


def now_ts() -> int:
    return int(time.time())
